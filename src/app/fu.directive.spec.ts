import { FuDirective } from './fu.directive';

describe('FuDirective', () => {
  it('should create an instance', () => {
    // const directive = new FuDirective();
    // expect(directive).toBeTruthy();
  });
});
